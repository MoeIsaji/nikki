<%= image_tag customer.get_profile_image, size:'100x100', :class => "rounded-circle" %>
<h2><%= customer.user_name %> さん</h2>
<table class='table table-borderless table_border_radius'>
  <tr>
	  <th><%= customer.introduction %></th>
  </tr>
  <tr>
  　<th><%= link_to customer.followings.count, customer_followings_path(customer) %><div class="ml-1 d-inline-block"></div>フォロー</th><th><%= link_to customer.followers.count, customer_followers_path(customer) %><div class="ml-1 d-inline-block"></div>フォロワー</th>
  </tr>
  </thread>
</table>

<div class='row'>
  <% if current_customer!= customer %>
    <td><%= render "public/relationships/btn", customer: customer %></td>
  <% else %>
    <%= link_to edit_customer_path(customer), class: "btn btn-outline-secondary btn-block edit_customer_#{customer.id}" do %>
    <i class="fas fa-customer-cog"></i>編集する
    <% end %>
    <% end %>
</div>