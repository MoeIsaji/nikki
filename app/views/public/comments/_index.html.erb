<table>
  <tbody>
      <% article.comments.each do |comment| %>
      <tr>
        
        <td>
          <%= link_to customer_path(comment.customer) do %>
            <%= image_tag comment.customer.get_profile_image, size: "100x100" %><br>
            <%= comment.customer.user_name %>
          <% end %>
        </td>
        <td><%= comment.comment %></td>
        <td>
          <% if comment.customer == current_customer %>
            <%= link_to "Destroy", article_comment_path(article, comment), method: :delete, class: "btn btn-danger pull-right", remote: true %>
          <% end %>
        </td>
      </tr>
      <% end %>
  </tbody>
</table>